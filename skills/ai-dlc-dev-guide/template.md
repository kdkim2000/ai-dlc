# 개발자 가이드 — {사업명}

| 항목 | 내용 |
|:---|:---|
| 사업명 | {사업명} |
| 작성일 | {YYYY-MM-DD} |
| 작성자 | {작성자} |
| 버전 | v1.0 |

---

## 버전 이력

| 버전 | 일자 | 작성자 | 변경 내용 |
|:---:|:---:|:---:|:---|
| v1.0 | {YYYY-MM-DD} | {작성자} | 최초 작성 |

---

## 목차

1. [개발 환경 설정](#1-개발-환경-설정)
2. [프로젝트 구조](#2-프로젝트-구조)
3. [API 참조](#3-api-참조)
4. [데이터베이스 스키마](#4-데이터베이스-스키마)
5. [주요 비즈니스 규칙](#5-주요-비즈니스-규칙)
6. [배포 방법](#6-배포-방법)
7. [테스트 실행](#7-테스트-실행)

---

## 1. 개발 환경 설정

### 1.1 사전 요구사항 (Prerequisites)

| 도구 | 버전 | 설치 |
|:---|:---:|:---|
| {도구명} | {버전} | {설치 방법/URL} |
| {도구명} | {버전} | {설치 방법/URL} |

### 1.2 환경 설정 단계

```bash
# 1. 저장소 클론
git clone {저장소_URL}
cd {프로젝트명}

# 2. 의존성 설치
{npm install / mvn install / 기타}

# 3. 환경 변수 설정
cp .env.example .env
# .env 파일을 열어 필요한 값 입력

# 4. 데이터베이스 초기화
{DB 초기화 명령어}

# 5. 서버 실행
{실행 명령어}
```

### 1.3 환경 변수

| 변수명 | 설명 | 예시 | 필수 |
|:---|:---|:---|:---:|
| `${변수명}` | {설명} | {예시값} | ✅/❌ |
| `${변수명}` | {설명} | {예시값} | ✅/❌ |

---

## 2. 프로젝트 구조

```
{프로젝트루트}/
├── {디렉터리}/          # {역할 설명}
│   ├── {하위디렉터리}/  # {역할 설명}
│   └── {파일}
├── {디렉터리}/          # {역할 설명}
└── {설정파일}           # {역할 설명}
```

### 주요 모듈 설명

| 모듈/패키지 | 역할 |
|:---|:---|
| `{모듈명}` | {역할 설명} |
| `{모듈명}` | {역할 설명} |

---

## 3. API 참조

### 3.1 공통 사항

- **Base URL**: `{baseUrl}`
- **인증 방식**: {JWT Bearer / API Key / 없음}
- **요청 헤더**:
  ```
  Content-Type: application/json
  Authorization: Bearer {token}
  ```
- **응답 형식**: JSON

### 3.2 엔드포인트 목록

| Method | Endpoint | 설명 | 인증 |
|:---:|:---|:---|:---:|
| GET | `{endpoint}` | {설명} | ✅/❌ |
| POST | `{endpoint}` | {설명} | ✅/❌ |
| PUT | `{endpoint}` | {설명} | ✅/❌ |
| DELETE | `{endpoint}` | {설명} | ✅/❌ |

### 3.3 엔드포인트 상세

#### `{METHOD} {endpoint}` — {설명}

**요청 파라미터**:

| 파라미터 | 위치 | 타입 | 필수 | 설명 |
|:---|:---:|:---|:---:|:---|
| `{name}` | path/query/body | {타입} | ✅/❌ | {설명} |

**요청 Body 예시**:
```json
{
  "{field}": "{value}"
}
```

**응답 예시**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "{field}": "{value}"
  }
}
```

**오류 응답**:

| HTTP 코드 | 오류 코드 | 설명 |
|:---:|:---:|:---|
| 400 | {코드} | {설명} |
| 401 | {코드} | {설명} |
| 404 | {코드} | {설명} |

---

## 4. 데이터베이스 스키마

### 4.1 ERD 요약

```
{ERD ASCII 다이어그램}

{테이블A} ─┐
           ├─ {관계} ─ {테이블B}
{테이블C} ─┘
```

### 4.2 테이블 상세

#### {테이블명}

> {테이블 설명}

| 컬럼명 | 타입 | PK | FK | NOT NULL | 설명 |
|:---|:---|:---:|:---:|:---:|:---|
| `{컬럼명}` | {타입} | ✅ | | ✅ | {설명} |
| `{컬럼명}` | {타입} | | ✅ | ✅ | {설명} |
| `{컬럼명}` | {타입} | | | | {설명} |

---

## 5. 주요 비즈니스 규칙

| BR-ID | 규칙명 | 내용 | 관련 기능 |
|:---:|:---|:---|:---|
| BR-001 | {규칙명} | {내용} | {관련기능} |

---

## 6. 배포 방법

### 6.1 {Docker / JAR / npm build} 배포

```bash
# 빌드
{빌드 명령어}

# 실행
{실행 명령어}
```

### 6.2 환경별 설정

| 환경 | 설정 파일 | 비고 |
|:---|:---|:---|
| 개발 | {파일명} | {비고} |
| 스테이징 | {파일명} | {비고} |
| 운영 | {파일명} | {비고} |

### 6.3 배포 체크리스트

- [ ] 환경 변수 설정 확인
- [ ] 데이터베이스 마이그레이션 완료
- [ ] 빌드 성공 확인
- [ ] 헬스체크 엔드포인트 응답 확인

---

## 7. 테스트 실행

```bash
# 단위 테스트
{테스트 실행 명령어}

# E2E 테스트
{E2E 테스트 명령어}

# 커버리지 리포트
{커버리지 명령어}
```

### 테스트 구조

| 테스트 유형 | 위치 | 실행 명령어 |
|:---|:---|:---|
| 단위 테스트 | {경로} | {명령어} |
| 통합 테스트 | {경로} | {명령어} |
| E2E 테스트 | {경로} | {명령어} |

---

## 부록

### 부록 A. 의존성 목록

| 라이브러리 | 버전 | 용도 |
|:---|:---:|:---|
| {라이브러리} | {버전} | {용도} |

### 부록 B. 관련 문서 링크

| 문서명 | 파일 |
|:---|:---|
| 요구사항 정의서 | {파일명} |
| API 설계서 | {파일명} |
| 데이터 설계서 | {파일명} |
